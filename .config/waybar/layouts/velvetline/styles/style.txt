/* ─────────────────────────────────────────────────────────────
   Matugen tokens (drop-in)
   ───────────────────────────────────────────────────────────── */

/* Autogenerated by matugen - do not edit */
@define-color background            #111318;
@define-color foreground            #e2e2e9;

@define-color primary               #acc7ff;
@define-color on_primary            #0f2f60;

@define-color secondary             #bec6dc;
@define-color tertiary              #debcdf;

@define-color error                 #ffb4ab;
@define-color on_error              #690005;

@define-color outline               #8e9099;
@define-color outline_variant       #44474f;

@define-color surface               #111318;
@define-color on_surface            #e2e2e9;

@define-color surface_variant       #44474f;
@define-color on_surface_variant    #c4c6d0;

@define-color surface_container_low   #111318;
@define-color surface_container       #44474f;
@define-color surface_container_high  #111318;

@define-color primary_fixed_dim       #acc7ff;
@define-color on_primary_fixed        #0f2f60;

/* Optional: alpha helpers */
@define-color surfaceA rgba(17, 19, 24, 0.85);
@define-color surface_variantA rgba(68, 71, 79, 0.85);


/* ─────────────────────────────────────────────────────────────
   Waybar styling using ONLY matugen variables
   ───────────────────────────────────────────────────────────── */

* {
  border: none;
  border-radius: 0;
  font-family: "Nasalization", "JetBrainsMono Nerd Font", sans-serif;
  font-size: 12px;
  font-weight: 700;
  min-height: 0;
}

/* Bar container */
window#waybar {
  background: transparent;
  color: @foreground;
  padding: 2px 0;
  margin: 4px 0 0 0;
}

/* ───────── Module containers ───────── */

#hyprland-workspaces,
#workspaces,
#custom-os_button,
#clock,
#custom-weather,
#custom-notification,
#custom-hypridle,
#tray,
#cpu,
#pulseaudio,
#network,
#custom-pacman,
#custom-media,
#custom-mpd {
  background: @surface_variantA;          /* was @bg2 */
  color: @on_surface_variant;             /* was @fg1 */

  padding: 4px 12px;
  margin: 0 4px;

  border-radius: 12px;
  border: 1px solid @outline;             /* was @fg1 */
}

/* ───────── Workspace font fallback ───────── */

#hyprland-workspaces,
#workspaces,
#hyprland-workspaces button,
#workspaces button {
  font-family:
    "Noto Sans CJK JP",
    "Noto Sans Symbols2",
    "JetBrainsMono Nerd Font",
    sans-serif;
}

/* Workspace buttons */

#hyprland-workspaces button,
#workspaces button {
  background: @surface_variantA;
  color: @error;                          /* was @red */
  border-radius: 12px;
  border: 2px solid @outline_variant;     /* was @bg1 */
  padding: 4px 12px;
  margin: 0 4px;
  transition: 0.15s ease;
}

#hyprland-workspaces button.active,
#workspaces button.active {
  background: @surface_variantA;
  color: @foreground;
  border: 2px solid @foreground;
}

#hyprland-workspaces button:hover,
#workspaces button:hover {
  background: @surface_variantA;
  color: @on_surface_variant;
}

/* ───────── Module accents (matugen only) ───────── */

#custom-os_button { color: @error; }
#custom-notification { color: @tertiary; }
#custom-weather { color: @primary; }
#cpu { color: @secondary; }
#pulseaudio { color: @primary; }
#network { color: @primary_fixed_dim; }
#custom-pacman { color: @tertiary; }
#custom-hypridle { color: @tertiary; }

/* ───────── Tray spacing ───────── */

#tray {
  letter-spacing: 0.5px;
}

#tray > .item {
  padding: 0 4px;
}

/* ───────── Text tracking ───────── */

label.module {
  letter-spacing: 2px;
}

/* Clock tuning */

#clock {
  letter-spacing: 0.8px;
  font-weight: 600;
  color: @on_surface_variant;   /* FIX: was invalid */
}

/* ───────── Media states ───────── */

#custom-mpd.on { color: @primary_fixed_dim; }
#custom-mpd.off { color: @outline; }

#custom-media.playing { color: @tertiary; }
#custom-media.paused  { color: @on_surface_variant; }
#custom-media.stopped { color: @outline; }